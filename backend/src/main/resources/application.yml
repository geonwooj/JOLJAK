spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  h2:
    console:
      enabled: true
      path: /h2-console

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 25MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: joljak5114@gmail.com
    password: "mjco hnxf fuus ojhg"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

logging:
  level:
    root: WARN
    org.springframework: WARN
    org.hibernate: WARN
    org.hibernate.SQL: OFF
    org.hibernate.orm.jdbc.bind: OFF
    com.joljak.backend: WARN

app:
  mail:
    from: "AI Patent Office <joljak5114@gmail.com>"
  email-verification:
    expiry-minutes: 5

file:
  upload-dir: uploads/chat